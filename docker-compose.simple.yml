version: '3.8'

services:
  # PostgreSQL for application state
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: testis
      POSTGRES_USER: testis
      POSTGRES_PASSWORD: testis_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U testis"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis for caching and queues
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ClickHouse for analytics - simplified version
  clickhouse:
    image: clickhouse/clickhouse-server:latest
    environment:
      CLICKHOUSE_DB: testis
      CLICKHOUSE_USER: testis
      CLICKHOUSE_PASSWORD: testis_password
    ports:
      - "8125:8123"  # Changed from 8124 to 8125
      - "9002:9000"  # Changed from 9001 to 9002
    volumes:
      - clickhouse_data:/var/lib/clickhouse
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8123/ping"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 60s

  # Collector service
  collector:
    build:
      context: .
      dockerfile: apps/collector/Dockerfile
    ports:
      - "3001:3001"
    environment:
      - REDIS_URL=redis://redis:6379
      - NODE_ENV=production
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Worker service
  worker:
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    environment:
      - REDIS_URL=redis://redis:6379
      - DATABASE_URL=postgresql://testis:testis_password@postgres:5432/testis
      - CLICKHOUSE_URL=http://clickhouse:8123
      - CLICKHOUSE_DATABASE=testis
      - CLICKHOUSE_USERNAME=testis
      - CLICKHOUSE_PASSWORD=testis_password
      - NODE_ENV=production
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
      clickhouse:
        condition: service_healthy

  # Web dashboard
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://testis:testis_password@postgres:5432/testis
      - CLICKHOUSE_URL=http://clickhouse:8123
      - CLICKHOUSE_DATABASE=testis
      - CLICKHOUSE_USERNAME=testis
      - CLICKHOUSE_PASSWORD=testis_password
      - NODE_ENV=production
    depends_on:
      postgres:
        condition: service_healthy
      clickhouse:
        condition: service_healthy

volumes:
  postgres_data:
  redis_data:
  clickhouse_data:

networks:
  default:
    name: testis-network